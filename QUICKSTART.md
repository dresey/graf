# 🎯 График — Инструкция по запуску

## ✅ Текущий статус

Серверы запущены и работают:
- **API-сервер:** http://127.0.0.1:3000 ✅
- **Фронтенд:** http://127.0.0.1:8080 ✅
- **Токен команды:** `GrfK2026-9mX4pL8qWnZ3vB7jR5hT6sN`

---

## 🚀 Начать работу (локально)

### 1. Открой приложение
Перейди в браузере:
```
http://localhost:8080
```

### 2. Введи токен команды
При первом открытии появится форма входа. Введи:
```
GrfK2026-9mX4pL8qWnZ3vB7jR5hT6sN
```

### 3. Проверь синхронизацию
Открой приложение в **двух разных браузерах** (Chrome + Edge) или в **обычном + инкогнито режиме**.

Измени график в одном окне → изменения должны появиться в другом через 1-2 секунды.

---

## 📱 Доступ с телефона (та же Wi-Fi сеть)

### Узнай IP ноутбука:
```cmd
ipconfig
```
Найди "IPv4-адрес", например: `192.168.1.92`

### Открой на телефоне:
```
http://192.168.1.92:8080
```

Телефон и ноутбук должны быть **в одной Wi-Fi сети**.

---

## 🌐 Доступ из интернета (Tailscale)

Для доступа из любой точки мира нужен Tailscale Funnel:

### 1. Установи Tailscale
https://tailscale.com/download/windows

### 2. Включи Funnel
PowerShell **от админа**:
```powershell
tailscale funnel 3000
```

Получишь URL вида: `https://твой-хост.ts.net`

### 3. Обнови server-config.js
```javascript
window.GRAFIK_API_URL = 'https://твой-хост.ts.net';
```

### 4. Обнови .env
```env
ALLOWED_ORIGINS=https://dresey.github.io,https://твой-хост.ts.net
```

### 5. Перезапусти сервер
```cmd
taskkill /F /IM node.exe
start-server.bat
```

### 6. Загрузи на GitHub Pages
Закоммить файлы (кроме `.env`!) и запушить.

---

## 🎮 Управление серверами

### Остановить всё:
```cmd
taskkill /F /IM node.exe
```

### Запустить API:
```cmd
start-server.bat
```

### Запустить фронтенд (для локального тестирования):
```cmd
npx http-server -p 8080 -c-1
```

---

## 🐛 Проблемы и решения

### "Не запрашивается токен"
✅ **Решение:** Используй `http://localhost:8080`, а не `file:///`. Токен запрашивается только когда в `server-config.js` указан корректный URL сервера.

### "Данные не синхронизируются"
✅ **Проверь:**
1. Оба устройства используют **один API URL** (проверь `server-config.js`)
2. API-сервер запущен: `curl http://localhost:3000/api/health`
3. В `.env` добавлен origin устройства в `ALLOWED_ORIGINS`
4. После изменения `.env` перезапусти сервер

### "CORS error"
✅ **Решение:** Добавь origin в `.env`:
```env
ALLOWED_ORIGINS=http://localhost:8080,http://192.168.1.92:8080
```
Перезапусти API-сервер.

### "Порт 3000 занят"
✅ **Решение:**
```cmd
netstat -ano | findstr :3000
taskkill /PID <номер> /F
```

---

## 📋 Чек-лист настройки

**Локальное тестирование:**
- [x] Серверы запущены
- [ ] Открыл http://localhost:8080
- [ ] Ввёл токен `GrfK2026-9mX4pL8qWnZ3vB7jR5hT6sN`
- [ ] Проверил синхронизацию в двух браузерах

**Публичный доступ:**
- [ ] Tailscale установлен
- [ ] Funnel включен и получен HTTPS URL
- [ ] Обновлён `server-config.js` с реальным URL
- [ ] Обновлён `.env` с новым origin
- [ ] Перезапущен сервер
- [ ] Файлы загружены на GitHub Pages
- [ ] Токен передан команде

---

**Следующий шаг:** Открой http://localhost:8080 и протестируй! 🎉
